This is lua-openssl from https://github.com/zhaozg/lua-openssl .

As the repository contains submodules, the archive is generated with the following commands:
```
huhn@asteroid:/tmp$ git clone --recursive https://github.com/zhaozg/lua-openssl.git
huhn@asteroid:/tmp$ rm -rf lua-openssl/.git
huhn@asteroid:/tmp$ tar cfvz lua-openssl-GIT3ed89e68d517.tar.gz lua-openssl
```
